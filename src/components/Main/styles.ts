import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 16,
    paddingHorizontal: 12,
    marginTop: 50,
    borderRadius: 5,
    alignSelf: 'center',
    width: 375,
    maxWidth: '100%',
  },
});
